<h1><%= @artist.id ? "Edit Artist" : "Create Artist"%></h1>
<%= form_for [@billboard_chart, @artist] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name%>
  <%= f.label :genre %>
  <%= f.text_field :genre%>
  <%= f.submit %>
<% end %>